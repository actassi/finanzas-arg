// Vitest setup file
// Add any global test setup here

// Increase timeout for PDF/OCR tests
import { vi } from "vitest";

// Mock console.error for cleaner test output (optional)
// vi.spyOn(console, 'error').mockImplementation(() => {});
